{
  "type" : "module",
  "scripts": {
    "build": "rimraf ./build && npx tsc",
    "start": "npm run build && node build/index.mjs",
    "dev" : "npx nodemon"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
